## $Id$
AUTOMAKE_OPTIONS=foreign no-dependencies
SUBDIRS = src docs tools

INCLUDES = @INCLUDES@

EXTRA_DIST = ChangeLog COPYING README RELEASE.NOTES VERSION \
	aclocal.m4 config.guess config.status config.sub \
	install-sh missing mkinstalldirs

DISTCLEANFILES=stamp-h.in
